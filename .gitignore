# list generated by create_list.sh with duplicates
list.txt
# list cleared out of duplicates
filtered_list.txt
# manually edited list to remove folders like 'unorganised' and '__oth'
filtered_list_cleared.txt
# list with regex appended to them
regex_list.txt
